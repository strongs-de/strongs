/**
 * Database schema.
 *
 * Tables are declared here; migrations are generated from this file with `pnpm db:generate` and are
 * never hand-edited once applied. See docs/architecture.md for the data model.
 */

export {};
