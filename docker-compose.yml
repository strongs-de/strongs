version: '2'
services:
#  db:
#    image: postgres
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    expose:
      - 8000
    environment:
      VIRTUAL_HOST: test2.wordsabout.de
#    depends_on:
#      - db
